NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
DATABASE_URL=

# Optional — only needed to run the QA seed script (`npm run db:seed:qa`).
# Credentials for the dummy QA account it creates/refreshes.
QA_EMAIL=
QA_PASSWORD=
